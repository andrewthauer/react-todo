import * as React from 'react';
import {
  TodoListContainer
} from '@app/todos';

const Dashboard = () => (
  <div>
    <h2>Dashboard</h2>
    <TodoListContainer />
  </div>
);

export default Dashboard;
