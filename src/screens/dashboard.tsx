import * as React from 'react';
import {
  TodoListContainer
} from '@app/todos';

export const Dashboard = () => (
  <div>
    <h2>Dashboard</h2>
    <TodoListContainer />
  </div>
);

export default Dashboard;
